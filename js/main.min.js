const navToggle = document.getElementById('nav-toggle');

navToggle.addEventListener('click', function() {
    this.classList.toggle('nav-toggle--active');
});

const slider = new Swiper('.page__slider', {

    slidesPerView: 1,
    spaceBetween: 10,
    pagination: {
        el: '.page__pagination',
        clickable: true,
    },
    navigation: {
        prevEl: '.page__nav-button--prev',
        nextEl: '.page__nav-button--next'
    }
  })